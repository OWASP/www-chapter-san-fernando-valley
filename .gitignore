__pycache__
env
.vscode

# Local Jekyll build output and caches. GitHub Pages builds the published site,
# so these only ever appear if someone runs Jekyll locally.
_site/
.jekyll-cache/
.sass-cache/
.bundle/
vendor/
